line-length = 100
target-version = "py311"
exclude = [
  ".git",
  "__pycache__",
  "*.egg-info",
  "venv",
  ".venv",
]

[lint]
select = ["E", "W", "F", "I", "N"]
ignore = [
  "E501",
  "E402",
  "F401",
]

[lint.isort]
known-first-party = ["src"]
