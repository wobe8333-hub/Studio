# ─── AI / Google ───────────────────────────────────────────────
google-generativeai>=0.7.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# ─── 영상 / 오디오 ──────────────────────────────────────────────
yt-dlp>=2024.1.0
manim>=0.18.0
ffmpeg-python>=0.2.0
pydub>=0.25.1
gTTS>=2.3.0

# ─── 기반 유틸 ──────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
tenacity>=8.2.0
pydantic>=2.0.0
schedule>=1.2.0
psutil>=5.9.0
filelock>=3.13.0

# ─── 로깅 / 캐시 / 모니터링 ──────────────────────────────────────
loguru>=0.7.0
sentry-sdk>=2.0.0
diskcache>=5.6.0

# ─── 트렌드 / 지식 수집 ──────────────────────────────────────────
praw>=7.7.0
pytrends>=4.9.0
tavily-python>=0.3.0
feedparser>=6.0.0
beautifulsoup4>=4.12.0
httpx>=0.27.0

# ─── Supabase DB 동기화 ──────────────────────────────────────────
supabase>=2.0.0

# ─── 이미지 처리 ─────────────────────────────────────────────────
Pillow>=10.0.0

# ─── GPU 선택 패키지 (SD XL + LoRA) ─────────────────────────────
# GPU 환경에서만 설치: pip install torch diffusers transformers accelerate safetensors
# CI/CD 및 CPU-only 환경에서는 아래 줄 주석 처리
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0

# ─── 음성 / 자막 ─────────────────────────────────────────────────
elevenlabs>=1.0.0
faster-whisper>=1.0.0

# ─── 테스트 ──────────────────────────────────────────────────────
pytest>=8.0.0
pytest-mock>=3.12.0

# ─── 벡터 메모리 (v10.0) ────────────────────────────────────────
sentence-transformers>=5.4.0   # 로컬 임베딩 모델 (numpy 코사인 유사도, Windows 지원)
# sqlite-vss                   # Linux/macOS 전용 — Windows 미지원, numpy로 대체
